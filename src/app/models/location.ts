export interface Location {
    id: string; // UUID
    name: string;
    address?: string; // Nullable
    zip?: string; // Nullable
    city?: string; // Nullable
    country?: string; // Nullable
  }
  